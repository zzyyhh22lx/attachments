
class treeSelectOffscreenCanvas {
    canvas: HTMLCanvasElement;
    height: number;
    width: number;
    constructor(canvas: HTMLCanvasElement) {
        this.canvas = canvas;
        this.height = canvas.height;
        this.width = canvas.width;
    }
    /** canvas自适应高度 */
    adaptiveHeight() {
        
    }
}
