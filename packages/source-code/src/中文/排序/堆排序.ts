// 堆排序：
// 先建立最大堆(第一个元素值最大)(注意，第0个元素我们自己注入,不然判断最大堆的时候第0个元素计算父子元素比较麻烦)
// 交换第一个元素和最后一个元素(删除最后一个 length--)，并maxHeapify(1)，当length<1则排序ok
// 最大堆 递归搜索左右子节点,如果大数大于父节点值，则交换
// 原地堆排序
function sortArray(nums: number[]): number[] {


    return nums;
}
function maxHeap() {
    
}