/**
哈希表+反向遍历
一个字符串：“050004003”，进行切割，找出它的递减数组（每次减一），如：[5,4,3]。
如果是 0501004003则[5,4,3]或者[1,0]都可以
 */
function findDecreasingSequences(str: string) {
    const len = str.length;
    const dp = new Array(len);
    
}
